import _filter from "lodash/filter";
import { notDatum } from '../utils'; // same as _.compact, but keep the zeros, they are important for dataVis

export default function compactData(array) {
  return _filter(array, function (d) {
    return !notDatum(d);
  });
}