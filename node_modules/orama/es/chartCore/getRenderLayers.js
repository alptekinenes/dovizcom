import _map from "lodash/map";

/*
generate the renderLayers and renderData by running the plot functions of the props root and the props.layers
*/
export function getLayer(props) {
  var _props$plot = props.plot,
      plot = _props$plot === void 0 ? function () {
    return [];
  } : _props$plot;
  return {
    layerProps: props,
    renderData: plot(props)
  };
}
export var getRenderLayers = function getRenderLayers(props) {
  return _map(props.layers, function (layer) {
    return getLayer(Object.assign({}, props, layer));
  });
};