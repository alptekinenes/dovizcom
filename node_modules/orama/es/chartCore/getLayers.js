import _get from "lodash/get";
import { Children } from 'react';
export default function getLayers(props) {
  var layers = Children.map(props.children, function (layer) {
    var plot = _get(layer, 'type.plot') || _get(layer, 'props.plot');

    return plot ? Object.assign({
      plot: plot
    }, layer.props) : undefined;
  });
  return {
    layers: layers
  };
}