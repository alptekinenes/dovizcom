// Copyright 2018 Kensho Technologies, LLC.
export var windowMock = {
  innerWidth: 1000,
  innerHeight: 1000,
  open: function open() {
    return undefined;
  }
};
export function getWindow() {
  if (global.window) return global.window;
  return windowMock;
}